{
  "type": "module",
  "scripts": {
    "build:local": "node tools/make-local-visualizator.cjs",
    "check:local": "npm run build:local && node tools/check-local-build.cjs",
    "test": "node --test tests/*.test.js",
    "check": "npm run test && npm run check:local"
  }
}
